[www]
user = www-data
group = www-data

; リッスンするポート（WordPress Docker コンテナはこのポートで待ち受け）
listen = 9000
listen.owner = www-data
listen.group = www-data
listen.mode = 0660

; プロセスマネージャの設定
pm = dynamic
pm.max_children = 10
pm.start_servers = 2
pm.min_spare_servers = 1
pm.max_spare_servers = 3

chdir = /
